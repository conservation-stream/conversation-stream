








### Internal
Internal packages are never to be published and are just used to share code between apps & packages. They use the `workspace:` protocol to reference each other and don't have a build step. The typescript files are referenced directly.